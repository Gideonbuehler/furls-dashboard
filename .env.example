# Database Connection (PostgreSQL)
# For local PostgreSQL:
DATABASE_URL=postgresql://postgres:password@localhost:5432/furls
# For Render.com database (get from Render Dashboard > Database > Connection String):
# DATABASE_URL=postgres://user:password@host:5432/database

# Server Configuration
NODE_ENV=development
PORT=3002

# JWT Secret - Change this to a random string in production
JWT_SECRET=your-secret-key-change-this-in-production-use-random-string

